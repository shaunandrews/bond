import { FELIX_DEFINITION } from './felix'
import { Q_DEFINITION } from './q'

/** Bundled agent definitions, parsed by the same parser as user files. */
export const BUILTIN_AGENT_SOURCES: string[] = [FELIX_DEFINITION, Q_DEFINITION]
